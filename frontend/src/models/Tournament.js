export class Tournament{
    constructor(id, bracket, endDate, format, logo, name, startDate) {
        this.id = id;
        this.bracket = bracket;
        this.endDate = endDate;
        this.format = format;
        this.logo = logo;
        this.name = name;
        this.startDate = startDate;
    }
}